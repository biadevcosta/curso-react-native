const socialNetwork = ['facebook','instagram', 'tiktok']

socialNetwork.forEach(function funcao (item,index,array){
    console.log()
})

/*socialNetwork.forEach((item, index, array) => {
    console.log(`${index+1})${item}`)
    console.log(array)
})*/

